import React from 'react';
import Typography from '@bit/mui-org.material-ui.typography';

export const Header = () => (
  <header>
    <Typography component="h1" variant="h2" gutterBottom>World’s top 20 polluters</Typography>
  </header>
);
